<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @category.title %>
</p>

<% if @category.parent_id  %>
<p>
  <b>Parent:</b>
  <%= Category.find_by_id(@category.parent_id).title%>
</p>
<% end %>


<%= link_to 'Edit', edit_admin_category_path(@category) %> |
<%= link_to 'Back', admin_categories_path %>
